# Runtime Context Protocol v1 (Deprecated)

> **DEPRECATED:** This document is superseded by `docs/PITH_RUNTIME_CONTEXT_PROTOCOL_V1.md`.  
> Please use the Pith-specific runtime context protocol as the single source of truth.

---

Этот файл сохраняется только для обратной совместимости со старыми ссылками и упоминаниями.

Актуальная версия протокола находится здесь:

- `docs/PITH_RUNTIME_CONTEXT_PROTOCOL_V1.md`

Используйте её как единственный канонический источник:

- определения источников контекста,
- порядка и приоритетов сборки,
- правил pruning,
- режим-зависимого поведения (`NORMAL`, `DIAGNOSTICS`, `VISION`),
- требований к `ContextAssembler` и `RuntimePlanner`.
